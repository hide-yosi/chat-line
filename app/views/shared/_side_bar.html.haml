.left-bar
  .left-bar__up
    .left-bar__up--name
      = current_user.name
    .fa
      = link_to do
        = icon('fas', 'pen-square', class: 'icon1')
    .fa
      = link_to edit_user_path(current_user) do
        = icon('fas', 'cog', class: 'icon')

  .left-bar__down
    .left-bar__down--group
      group
      .left-bar__down--group--text
        message
    .left-bar__down--group
      group
      .left-bar__down--group--text
        message